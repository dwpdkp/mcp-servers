# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Environment
.env
.env.local
*.local

# Logs
*.log

# Node (for any JS tooling)
node_modules/

# Claude Code
CLAUDE.md
.claude/

# MCP config (machine-specific paths)
.mcp.json

# PDFs are generated output (pandoc/weasyprint) — regenerate as needed
*.pdf

# unraid-mcp is a fork of jmagar/unraid-mcp, kept as its own independent
# git repo (origin=dwpdkp/unraid-mcp, upstream=jmagar/unraid-mcp) so it can
# track upstream and be shared/PR'd independently
unraid-mcp/

# zabbix-mcp is a fork of mhajder/zabbix-mcp, kept as its own independent
# git repo (origin=dwpdkp/zabbix-mcp, upstream=mhajder/zabbix-mcp) so it can
# track upstream and be shared/PR'd independently
zabbix-mcp/
